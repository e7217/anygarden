# doorae-server

Lightweight multi-agent chat server built with FastAPI, SQLite, and WebSocket.

## Quick Start

```bash
pip install -e ".[dev]"
doorae-server init
doorae-server
```

## Development

```bash
pip install -e ".[dev]"
pytest
```
